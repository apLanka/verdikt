{
  "name": "@verdikt/investigator",
  "version": "0.1.0",
  "description": "Investigator agent Lambda — cross-checks extracted facts against policy limits and prior claims",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-sfn": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@aws-sdk/client-bedrock-runtime": "^3.600.0",
    "@verdikt/contracts": "file:../../contracts"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
